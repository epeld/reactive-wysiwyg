;; TODO from some kind of blueprint (JSON?) create some kind of graph with a WS-signal on top!


;; TOP Signal JSON
;; 2nd Store Signals; filter JSON keys and compare values
;; Then: standard signal graph

;; { intSignal: { intSignalSubSignal: { fn: "add" } } }
